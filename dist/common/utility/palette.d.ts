/**
 * Creates a new palette from a single color.
 *
 * @param name Name of the variant.
 * @param color 6-character HEX code, starting with '#'.
 */
export declare function createPalette(name: string, color: string): {
    [x: string]: string;
};
